# Levantar de forma independiente (asegúrate de levantar la app principal primero para que exista la red):
#   cd monitoring && docker compose -f docker-compose.monitoring.yml up -d

name: avatars-generator-monitoring

services:
  prometheus:
    image: prom/prometheus:v3.4.0
    container_name: avatars-generator-prometheus
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    ports:
      - "9090:9090"
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.retention.time=7d"
    networks:
      - avatars-generator-net

  grafana:
    image: grafana/grafana:11.6.0
    container_name: avatars-generator-grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana-data:/var/lib/grafana
      - ./monitoring/grafana/provisioning/datasources:/etc/grafana/provisioning/datasources:ro
      - ./monitoring/grafana/provisioning/dashboards:/etc/grafana/provisioning/dashboards:ro
      - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
    depends_on:
      - prometheus
    networks:
      - avatars-generator-net

volumes:
  prometheus-data:
  grafana-data:

networks:
  avatars-generator-net:
    external: true
    name: avatars-generator-net
