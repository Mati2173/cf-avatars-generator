apiVersion: apps/v1
kind: Deployment
metadata:
  name: avatars-generator-web
  labels:
    app: avatars-generator-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: avatars-generator-web
  template:
    metadata:
      labels:
        app: avatars-generator-web
    spec:
      containers:
        - name: web
          image: ghcr.io/mati2173/cf-avatars-generator-web
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
